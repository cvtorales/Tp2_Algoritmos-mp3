# ifndef SETUP__H
# define SETUP__H

typedef enum 
{
	TRACK_LIST_FORMAT_CSV,
	TRACK_LIST_FORMAT_XML
} track_list_format_t;

typedef enum
{
	TRACK_SORT_BY_NAME,
	TRACK_SORT_BY_ARTIST,
	TRACK_SORT_BY_GENRE
} track_sort_type_t;



# endif